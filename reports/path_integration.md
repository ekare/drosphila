# Path integration

status: **not_started**

evidence: Ordered SO(3) composition and a visual azimuth projection are
implemented as oracle-safe primitives. No scale-free trajectory RPE, metric
trajectory RPE, reset/re-anchor, or held-out loop experiment has been run.

blocker: Upstream relative rotation and translation are not accepted.

next_required_condition: Freeze accepted relative pose inputs and evaluate
drift, re-anchoring, and uncertainty on trajectory-disjoint sequences.
