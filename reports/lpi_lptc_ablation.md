# LPi/LPTC ablation

status: **not_started**

evidence: Literature mapping and architecture slots are documented, but no
LPi-like or LPTC-template model has been trained or accepted.

blocker: The current local field fails the direction gate, so adding a
downstream wide-field readout would not identify a causal improvement.

next_required_condition: Repair the field, freeze the frontend, and compare
baseline, LPi-like opponency, and LPTC template branches on identical splits.
